# AGENT.md

このリポジトリで作業する AI エージェント向けのガイド（[`CLAUDE.md`](CLAUDE.md) と同内容）。

## プロジェクト

ベランダで釣りをする見下ろし視点のブラウザゲーム（**TypeScript + Phaser 3 + Vite**）。
現在は **1280×720 への作り直し中**で、いま動く新方向は歩行サンドボックス（`npm run dev` 後 `http://localhost:5173/sandbox.html`）。
機能ドキュメントは [`docs/README.md`](docs/README.md)（機能一覧）と [`docs/TODO.md`](docs/TODO.md)（全体ロードマップ）、全体像は [`README.md`](README.md) を参照。

## このプロジェクトの担当者と進め方

担当者は **ClaudeCode 初心者・非エンジニア**。手伝うときは次を守ること:

- 専門用語はできるだけ避け、使うときは必ずやさしい言葉で補足する。
- 変更は「何を・なぜ」を先に日本語で説明してから着手する。
- 判断が必要なときは、選択肢とそれぞれの長所・短所を、非エンジニアでも選べる粒度で提示する。
- 一気に進めず、ステップごとに「動いて見える」状態を見せながら確認する。
- git で安全に元へ戻せることを前提に、安心して試せるよう導く。

## このゲームの位置づけ（重要）

現在の実装は **叩き台（プロトタイプ）**。確定しているのは基本要素のみ:
**通常マップ / 図鑑 / 釣りゲーム**。

今後、詳細なゲーム要件を改めて整理した上で **一から作り直すことを検討中**。
技術スタック（**TypeScript + Phaser 3 + Vite**）は維持する。
→ 当面は現行コードへの大きな作り込みより、まず**要件整理を優先**する。

## 開発コマンド

```bash
npm run dev        # 開発サーバ（http://localhost:5173/）
npm run typecheck  # 型チェック（tsc --noEmit）
npm run build      # 型チェック + 本番ビルド
node scripts/smoke.mjs   # 実機Chromeでのスモークテスト（要: dev サーバ起動）
```

変更後は最低でも `npm run typecheck` を通すこと。挙動に関わる変更は `node scripts/smoke.mjs` も実行する。

## ルール: ゲーム性に関わる調整は履歴を必ず残す

**ゲームバランス／挙動／操作フィールに関わる調整**（釣りのテンポ、判定の広さ・速さ、難易度、
ピクンや「間」のタイミング、メーター挙動、確率・重み付け など）を変更するときは、
**必ず [`docs/tuning-log.md`](docs/tuning-log.md) に履歴を残す**こと。

各エントリに以下を含める:

1. **日付** と **テイク名**（`take1`, `take2`, … と連番で増やす）
2. **フィードバック内容**（なぜ変えるのか。ユーザー要望や所感をそのまま）
3. **変更前 → 変更後** の具体的な値（数値・ゾーン・フラグ等を表で）
4. **該当コード箇所**（ファイル / 定数名）
5. **検証結果**（typecheck / smoke）

数値だけ書き換えて履歴を残さないのは禁止。理由と差分が後から追えるようにする。
（純粋なバグ修正・リファクタ・UI文言などゲーム性に影響しない変更はこのルールの対象外。）
