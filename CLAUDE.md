# CLAUDE.md

このリポジトリで作業する Claude Code 向けのガイド。

## プロジェクト

ベランダで釣りをする見下ろし視点のブラウザゲーム（**TypeScript + Phaser 3 + Vite**、内部解像度 320×180）。
仕様は [`docs/SPEC.md`](docs/SPEC.md)、全体像は [`README.md`](README.md) を参照。

## 開発コマンド

```bash
npm run dev        # 開発サーバ（http://localhost:5173/）
npm run typecheck  # 型チェック（tsc --noEmit）
npm run build      # 型チェック + 本番ビルド
node scripts/smoke.mjs   # 実機Chromeでのスモークテスト（要: dev サーバ起動）
```

変更後は最低でも `npm run typecheck` を通すこと。挙動に関わる変更は `node scripts/smoke.mjs` も実行する。

## ルール: ゲーム性に関わる調整は履歴を必ず残す

**ゲームバランス／挙動／操作フィールに関わる調整**（釣りのテンポ、判定の広さ・速さ、難易度、
ピクンや「間」のタイミング、メーター挙動、確率・重み付け など）を変更するときは、
**必ず [`docs/tuning-log.md`](docs/tuning-log.md) に履歴を残す**こと。

各エントリに以下を含める:

1. **日付** と **テイク名**（`take1`, `take2`, … と連番で増やす）
2. **フィードバック内容**（なぜ変えるのか。ユーザー要望や所感をそのまま）
3. **変更前 → 変更後** の具体的な値（数値・ゾーン・フラグ等を表で）
4. **該当コード箇所**（ファイル / 定数名）
5. **検証結果**（typecheck / smoke）

数値だけ書き換えて履歴を残さないのは禁止。理由と差分が後から追えるようにする。
（純粋なバグ修正・リファクタ・UI文言などゲーム性に影響しない変更はこのルールの対象外。）

同じ内容を [`AGENT.md`](AGENT.md) にも記載している（他エージェント向け）。
