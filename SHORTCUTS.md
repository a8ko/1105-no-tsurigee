# ショートカット一覧

よく使うキー操作のメモ。

| ショートカット | できること |
| --- | --- |
| `⌘ Command` + `⇧ Shift` + `V` | Markdown（.md）ファイルをプレビュー表示（見やすい形にビジュアライズ） |
| `⌥ Option` + `V` | 過去にコピーしたもの（クリップボード履歴）を呼び出す |
| `⌘` + `⇧ Shift` + `4` | スクリーンショット（範囲を選んで撮影） |

## Claude Code の /コマンド

入力欄で `/` を押すと一覧が出ます。初心者におすすめのものを厳選。

| コマンド | できること |
| --- | --- |
| `/clear` | これまでの会話をリセットして、まっさらな状態から始める（話が長くなって混ざってきたら） |
| `/resume` | 過去の会話を選んで再開する（昨日の続きをやりたいときなど） |
| `/help` | 使い方やコマンドの一覧を表示する |
| `/init` | プロジェクトの説明書（CLAUDE.md）を自動で作る・更新する |
| `/config` | 設定を変える（テーマや使うモデルなど） |
| `/model` | 使う AI モデルを切り替える |
| `/review` | プルリクエスト（変更のまとめ）をレビューしてもらう |
| `/effort` | AI の「考える深さ」を変える（`low`〜`max`。引数なしでスライダー表示） |

